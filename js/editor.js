// Editor class, has script, language, and theme properties.
class Editor {
    constructor(script, language, theme) {
        this.script = script;
        this.language = language;
        this.theme = theme;
    }
}